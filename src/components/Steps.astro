<div class="oiva-steps">
  <slot />
</div>

<style is:global>
  .oiva-steps {
    --step-content-gap: 2rem;
    --step-marker-border: 4px;
    --step-marker-size: 2rem;

    border-left: 1px solid var(--border-subtle);
    counter-reset: step;
    margin: var(--space-xl) 0 var(--space-2xl) 1rem;
    padding-left: var(--step-content-gap);
  }

  .oiva-steps > h3 {
    counter-increment: step;
    font-size: var(--text-base);
    line-height: var(--leading-ui);
    margin: var(--space-2xl) 0 var(--space-md);
    position: relative;
  }

  .oiva-steps > h3:first-child {
    margin-top: 0;
  }

  .oiva-steps > h3::before {
    background: var(--color-surface-muted);
    border: var(--step-marker-border) solid var(--color-surface);
    border-radius: 999px;
    box-sizing: border-box;
    color: var(--color-text);
    content: counter(step);
    display: grid;
    font-family: var(--font-mono);
    font-feature-settings: "tnum";
    font-variant-numeric: tabular-nums;
    font-size: var(--text-sm);
    font-weight: 750;
    height: var(--step-marker-size);
    left: calc(-1 * (var(--step-content-gap) + (var(--step-marker-size) / 2)));
    line-height: 1;
    place-items: center;
    position: absolute;
    text-align: center;
    top: -0.28rem;
    width: var(--step-marker-size);
  }

  .oiva-steps > p,
  .oiva-steps > ul,
  .oiva-steps > ol,
  .oiva-steps > pre {
    margin-top: var(--space-md);
  }

  .oiva-steps > pre {
    max-width: 100%;
  }

  @media (max-width: 640px) {
    .oiva-steps {
      --step-content-gap: 1.5rem;
      --step-marker-border: 3px;
      --step-marker-size: 1.75rem;

      margin-left: 0.75rem;
    }

    .oiva-steps > h3::before {
      font-size: 0.75rem;
      top: -0.18rem;
    }
  }
</style>
