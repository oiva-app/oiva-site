---
import DiagramCaption from "./DiagramCaption.astro";

type Props = {
  title?: string;
  eyebrow?: string;
  caption?: string;
  sourceLabel?: string;
  sourceHref?: string;
  modalSrc?: string;
  modalAlt?: string;
  variant?: "default" | "raised" | "outline";
  class?: string;
};

const {
  title,
  eyebrow,
  caption,
  sourceLabel,
  sourceHref,
  modalSrc,
  modalAlt,
  variant = "default",
  class: className = "",
} = Astro.props;
---

<figure class:list={["figure-card", `figure-card-${variant}`, className]}>
  {(title || eyebrow) && (
    <header class="figure-card-header">
      {eyebrow && <p>{eyebrow}</p>}
      {title && <h3>{title}</h3>}
    </header>
  )}

  <div class="figure-card-body">
    <slot />
    {modalSrc && (
      <button
        class="figure-card-modal-trigger"
        type="button"
        data-figure-modal-trigger
        data-figure-modal-src={modalSrc}
        data-figure-modal-alt={modalAlt ?? title ?? ""}
        aria-label={title ? `Enlarge ${title}` : "Enlarge figure"}
      ></button>
    )}
  </div>

  {caption && (
    <DiagramCaption sourceLabel={sourceLabel} sourceHref={sourceHref}>
      {caption}
    </DiagramCaption>
  )}
</figure>

<style>
  .figure-card {
    background: var(--color-surface);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
    color: var(--color-text);
    margin: var(--space-xl) 0;
    overflow: hidden;
    padding: var(--space-lg);
  }

  .figure-card-raised {
    background: var(--color-surface-raised);
  }

  .figure-card-outline {
    background: transparent;
    border-style: dashed;
    box-shadow: none;
  }

  .figure-card-header {
    border-bottom: 1px solid var(--border-subtle);
    margin-bottom: var(--space-md);
    padding-bottom: var(--space-sm);
  }

  .figure-card-header p {
    color: var(--color-primary);
    font-size: var(--text-xs);
    font-weight: 800;
    letter-spacing: 0;
    line-height: var(--leading-ui);
    margin: 0 0 var(--space-2xs);
    text-transform: uppercase;
  }

  .figure-card-header h3 {
    font-family: var(--font-sans);
    font-size: var(--text-md);
    line-height: var(--leading-heading);
    margin: 0;
  }

  .figure-card-body {
    min-width: 0;
    position: relative;
  }

  .figure-card-modal-trigger {
    background: transparent;
    border: 0;
    cursor: zoom-in;
    height: 100%;
    inset: 0;
    padding: 0;
    position: absolute;
    width: 100%;
    z-index: 2;
  }

  .figure-card-modal-trigger:focus-visible {
    border-radius: var(--radius-md);
    outline: 3px solid color-mix(in srgb, var(--color-primary) 32%, transparent);
    outline-offset: -3px;
  }
</style>
