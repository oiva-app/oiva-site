<div class="home-hero-atlas-background" aria-hidden="true"></div>

<style>
  .home-hero-atlas-background {
    background-color: color-mix(in srgb, var(--color-bg-soft) 76%, var(--color-surface));
    background-image:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--color-surface) 10%, transparent),
        transparent 30%,
        color-mix(in srgb, var(--color-bg-soft) 12%, transparent) 100%
      ),
      url("../assets/backgrounds/home-hero-light.webp");
    background-image:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--color-surface) 10%, transparent),
        transparent 30%,
        color-mix(in srgb, var(--color-bg-soft) 12%, transparent) 100%
      ),
      image-set(
        url("../assets/backgrounds/home-hero-light.avif") type("image/avif"),
        url("../assets/backgrounds/home-hero-light.webp") type("image/webp")
      );
    background-position: center;
    background-repeat: no-repeat;
    background-size: cover;
    inset: 0;
    -webkit-mask-image: linear-gradient(to bottom, black 0%, black 86%, transparent 100%);
    mask-image: linear-gradient(to bottom, black 0%, black 86%, transparent 100%);
    opacity: 1;
    overflow: hidden;
    pointer-events: none;
    position: absolute;
    width: 100%;
    z-index: 0;
  }

  .home-hero-atlas-background::before,
  .home-hero-atlas-background::after {
    content: "";
    inset: 0;
    position: absolute;
  }

  .home-hero-atlas-background::before {
    background:
      radial-gradient(
        ellipse at center 36%,
        color-mix(in srgb, var(--color-surface-raised) 8%, transparent) 0%,
        color-mix(in srgb, var(--color-surface-raised) 4%, transparent) 18%,
        transparent 32%
      );
  }

  .home-hero-atlas-background::after {
    background:
      linear-gradient(
        180deg,
        transparent 0%,
        transparent 62%,
        color-mix(in srgb, var(--color-bg) 5%, transparent) 100%
      );
    mix-blend-mode: normal;
  }

  :global([data-theme="dark"]) .home-hero-atlas-background {
    background-color: var(--color-bg);
    background-image:
      linear-gradient(
        180deg,
        rgba(6, 23, 22, 0.08),
        transparent 34%,
        rgba(6, 23, 22, 0.16) 100%
      ),
      url("../assets/backgrounds/home-hero-dark.webp");
    background-image:
      linear-gradient(
        180deg,
        rgba(6, 23, 22, 0.08),
        transparent 34%,
        rgba(6, 23, 22, 0.16) 100%
      ),
      image-set(
        url("../assets/backgrounds/home-hero-dark.avif") type("image/avif"),
        url("../assets/backgrounds/home-hero-dark.webp") type("image/webp")
      );
    opacity: 1;
  }

  :global([data-theme="dark"]) .home-hero-atlas-background::before {
    background:
      radial-gradient(
        ellipse at center 42%,
        rgba(6, 23, 22, 0.42) 0%,
        rgba(6, 23, 22, 0.24) 28%,
        transparent 58%
      );
  }

  :global([data-theme="dark"]) .home-hero-atlas-background::after {
    background:
      linear-gradient(
        180deg,
        rgba(6, 23, 22, 0.08) 0%,
        transparent 44%,
        rgba(6, 23, 22, 0.24) 100%
      );
    mix-blend-mode: normal;
  }

  @media (max-width: 920px) {
    .home-hero-atlas-background {
      background-position: center top;
      background-size: auto 100%;
      opacity: 1;
    }

    .home-hero-atlas-background::before {
      background:
        radial-gradient(
          ellipse at center 32%,
          color-mix(in srgb, var(--color-surface-raised) 10%, transparent) 0%,
          color-mix(in srgb, var(--color-surface-raised) 5%, transparent) 24%,
          transparent 38%
        );
    }

    :global([data-theme="dark"]) .home-hero-atlas-background::before {
      background:
        radial-gradient(
          ellipse at center 40%,
          rgba(6, 23, 22, 0.44) 0%,
          rgba(6, 23, 22, 0.26) 30%,
          transparent 62%
        );
    }
  }

  @media (max-width: 620px) {
    .home-hero-atlas-background {
      background-position: center top;
      background-size: auto 100%;
      opacity: 1;
    }
  }
</style>
